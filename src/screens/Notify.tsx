import {
  Box,
  Text,
  Input,
  Image,
  Select,
  CheckIcon,
  HStack,
  VStack,
  ScrollView,
  View,
  Button,
} from "native-base";
import { SafeAreaView } from "react-native-safe-area-context";

const Notify = (props: any) => {
  return 
};

export default Notify;
